public class EjerciciosAgustin1 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        //Realizar un algoritmo que resuelva los siguientes pasos
//Paso 1 Entrada de base y altura desde periférico de entrada por ejemplo teclado
//Paso 2 Cálculo de la superficie, multiplicando la base por la altura
//Pase 3 Salida por pantallas de base, altura y superficie.
        int base=Integer.parseInt(JOptionPane.showInputDialog("Ingrese la base:"));
        int altura = Integer.parseInt(JOptionPane.showInputDialog("Ingrese la a1tura:"));
        int superficie=base*altura;
        System.out.println(base);
        System.out.println(altura);
        JOptionPane.showMessageDialog(null,"la base es" + base);
        JOptionPane.showMessageDialog(null, "la a1ltura1 es:" + altura);
        JOptionPane.showInputDialog(null, "la superficie es" + superficie);
        .
